import { Historicosolicitacao } from './historicosolicitacao';

describe('Historicosolicitacao', () => {
  it('should create an instance', () => {
    expect(new Historicosolicitacao('','','','')).toBeTruthy();
  });
});
